#!/usr/bin/env bash
set -euo pipefail

echo "Browser verification is intentionally manual until a portable Playwright runner is added."
echo "Start the application with ./scripts/dev.sh and follow docs/PLAYTEST_CHECKLIST.md."
echo "Do not treat API-only checks as browser verification."
